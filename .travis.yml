language: java

jdk:
  - openjdk8

install:
  - mvn clean install -Dmaven.javadoc.skip=true -Dmaven.javadoc.skip=true -B -V

script:
  - cd wolf-flow-core
  - mvn test -B

after_success:
  - bash <(curl -s https://codecov.io/bash)